#include <iostream>
using namespace std;
int main()
{
    int number[10] = {1, 1, 2, 3, 4, 3, 4, 5, 2, 2};
    int size = sizeof(number) / sizeof(int);
    for (int i = 0; i < size; i++)
    {
        }

    return 0;
}